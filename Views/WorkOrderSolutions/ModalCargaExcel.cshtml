@model IEnumerable<AIBTicketsMVC.Models.NegacionLinea>


<div id="myModalCargarExcelNegacion" class="modal fade bd-example-modal-lg" tabindex="-1" role="dialog">
    <div class="modal-dialog modal-lg" role="document">
        <div class="modal-content">
            <form id="formTest" method="post" enctype="multipart/form-data">
                <div class="modal-header">
                    <h5 class="modal-title">Cargar definciones a lineas</h5>
                    <button id="closem" type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                @*<div class="modal-body">
                        <div class="row">
                            <div class="col-md-12">
                                <div class="card" style="font-size: 80%;">

                                    <input type="file" name="ArchivoExcel" id="ArchivoExcel" />f
                                </div>
                            </div>
                        </div>
                    </div>*@

                @*<div class="modal-footer">
                        <button type="button" class="btn btn-primary btn-sm btn-icon-split" id="BtnGuardar" onclick="CargarExcelNegacionLinea()">
                            <span class="text">Cargar Archivo</span>
                        </button>
                    </div>*@
            </form>

            @{
                var i = 1;
                long f = 0;
                foreach (var item in Model)
                {
                    if (i == 1)
                    {
                        <input type="text" id="ArchivoExcel" value="@item.IdWorkOrder" hidden />
                        f = @item.IdWorkOrder;
                        i = 0;
                    }
                }

            }

            @using (Html.BeginForm("CargarExcelNegacionLinea", "WorkOrderSolutions", new { IdWorkOrder = f }, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
                <input type="file" name="ArchivoExcel" />
                <input type="submit" value="Import" />

                i = 1;
                foreach (var item in Model)
                {
                    if (i == 1)
                    {
                        <input type="text" id="IdWorkOrder" value="@item.IdWorkOrder" hidden />
                        i = 0;
                    }
                }

                @*<button type="button" class="btn btn-primary btn-sm btn-icon-split" id="BtnGuardar" onclick="CargarExcelNegacionLinea()">
                        <span class="text">Cargar Archivo</span>
                    </button>*@

            }

        </div>
    </div>
</div>



