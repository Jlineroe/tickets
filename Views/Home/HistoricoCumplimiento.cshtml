@model IEnumerable<AIBTicketsMVC.Models.HistoryCompliance>


<div class="card">
    <h5 class="card-header"><i class="fas fa-calendar-check"></i> &nbsp Histórico Cumplimiento</i></h5>
    <div class="card-body">
        <table class="table table-striped table-bordered text-center">
            <thead>
                <tr>
                    <th scope="col">Mes</th>
                    <th scope="col">Total Cierres</th>
                    <th scope="col">Meta</th>
                    <th scope="col">Diferencia</th>
                    <th scope="col">Cumplimiento</th>
                </tr>
            </thead>
            <tbody>
                @{
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Semana</td>
                            <td>@item.Cantidad</td>
                            <td>@item.Meta</td>
                            <td>@item.Diferencia</td>
                            <td>@item.Cumplimiento %</td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>

@*<table id="HistoricoAsignados" class="table table-sm table-bordered table-hover" style="width:100%;" border="1">
    <thead>
        <tr style=" background-color: darkblue; color: white">
            <th>Mes</th>
            <th>Total Cierres</th>
            <th>Meta</th>
            <th>Diferencia</th>
            <th>Cumplimiento</th>
        </tr>
    </thead>
    <tbody>
        @{
            int par = 1;
            foreach (var item in Model)
            {
                var bg = "";
                var col = "";
                if (par == 0)
                {
                    bg = "cornflowerblue";
                    col = "black";
                    par = 1;
                }
                else
                {
                    bg = "white";
                    col = "black";
                    par = 0;
                }
                <tr style="background-color:@bg; color: @col;">
                    <td>@item.Semana</td>
                    <td>@item.Cantidad</td>
                    <td>@item.Meta</td>
                    <td>@item.Diferencia</td>
                    <td>@item.Cumplimiento</td>
                </tr>
            }
        }
    </tbody>
</table>*@


